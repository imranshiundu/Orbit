export class Sun {
  constructor() {
    this.name = "Sun";
    this.diameter = 1392700; // Real-world diameter in km
    this.mesh = null;
    this.light = null; // Point light
  }

  init(scene) {
    // Initialize mesh and add to scene
    // Add point light at center
  }
}
